#! /bin/bash

# Usar alias permite crear un sobrenombre a uno o mas comandos simultaneos. (esto funciona directamente en la terminal)
alias cls="clear; ls"

# Para eliminar un alias se usa unalias
unalias cls

# Para mantener los alias en el sistema se debera de agregar en el archivo: .bashrc
